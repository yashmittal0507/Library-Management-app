# Library-Management-app
A full-stack library management system built using NestJS, React (TypeScript), PostgreSQL, and Prisma, architected with a lightweight microservices approach.  The application provides a complete flow for managing books, authors, users, and borrowing operations with secure JWT-based authentication.
